# Local overrides: healthcheck simplificado para la DB
services:
  db:
    healthcheck:
      test: ["CMD-SHELL", "exit 0"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
